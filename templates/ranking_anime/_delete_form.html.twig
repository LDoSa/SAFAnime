<form method="post" action="{{ path('app_ranking_anime_delete', {'id': ranking_anime.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ranking_anime.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post"
      action="{{ path('app_anime_delete', {'id': anime.id}) }}"
      onsubmit="return confirm('¿Seguro que quieres borrar este anime?');"
      class="inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ anime.id) }}">
    <button class="px-5 py-2.5 rounded-lg bg-red-600 hover:bg-red-700 text-white font-medium shadow transition">
        Borrar
    </button>
</form>

<nav class="w-full bg-violet-300 text-violet-900 shadow-md">
    <div class="max-w-7xl mx-auto px-4 py-3 flex items-center justify-between">

        <a href="{{ path('home') }}" class="text-2xl font-bold tracking-wide">
            SAFAnime
        </a>

        <div class="hidden md:flex items-center gap-3">
            {% if app.user %}
                <a href="{{ path('app_logout') }}"
                   class="bg-white text-violet-900 px-4 py-2 rounded-lg border border-violet-400 hover:bg-violet-200 transition">
                    Cerrar sesión
                </a>
            {% else %}
                <a href="{{ path('app_register') }}"
                   class="bg-white text-violet-900 px-4 py-2 rounded-lg border border-violet-400 hover:bg-violet-200 transition">
                    Registrarse
                </a>

                <a href="{{ path('app_login') }}"
                   class="bg-violet-500 text-white px-4 py-2 rounded-lg hover:bg-violet-600 transition">
                    Iniciar sesión
                </a>
            {% endif %}
        </div>

        <button class="md:hidden text-violet-900 text-3xl">
            ☰
        </button>
    </div>
</nav>



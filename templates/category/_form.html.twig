{{ form_start(form, { attr: { class: 'space-y-6 max-w-xl mx-auto bg-violet-200 p-8 rounded-xl shadow-lg border border-violet-400' } }) }}

<div class="space-y-4">
    {{ form_widget(form, {
        attr: {
            class: 'w-full rounded-lg border-gray-300 focus:border-violet-600 focus:ring-violet-600'
        }
    }) }}
</div>

<button class="w-full mt-4 px-6 py-3 bg-violet-600 hover:bg-violet-700 text-white font-semibold rounded-lg shadow transition">
    {{ button_label|default('Guardar') }}
</button>

{{ form_end(form) }}

